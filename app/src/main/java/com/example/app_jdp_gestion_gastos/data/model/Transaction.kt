package com.example.app_jdp_gestion_gastos.data.modelimport java.util.*data class Transaction(    val id: String = UUID.randomUUID().toString(),    val description: String,    val amount: Double,    val date: String,    val type: String = if (amount < 0) "Gasto" else "Ingreso",  // Asignamos tipo basado en el monto    val icon: Int? = null  // Aquí agregamos el campo para el icono)